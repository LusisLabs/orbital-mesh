# Repo Truth Audit

This audit classifies the repository state as of 2026-05-06 from committed code, config, tests, and named docs. It is not live production clearance. Treat any live-readiness claim as unknown until rerun in the target environment.

## Confirmed Current Truths

- Active first-party Python runtime is packaged from `shared`, `services`, `mesh_brain`, and `simulation`, not from the source-input trees. Evidence: `pyproject.toml:32-53`.
- Active first-party runtime directories are `services/`, `shared/mesh_runtime/`, `mesh_brain/`, `control_plane_server.py`, `meshapp/`, `web/`, `scripts/`, `config/`, `policies/`, and `docker-compose*.yml`. Evidence: `AGENTS.md:42-51`, `pyproject.toml:32-53`, `control_plane_server.py:126-220`, `control_plane_server.py:300-518`.
- The HTTP API and SSE server are `control_plane_server.py` with `RunCoordinator` in `services/control_plane.py`. Confirmed routes include health, readiness, policy lifecycle, connector certification, deployment compatibility, failure modes, approvals, kill switch, pilot go/no-go, run details, Merkle, timeline proof, watchers, trust ladder, vault, run streams, and system stream. Evidence: `control_plane_server.py:126-220`, `control_plane_server.py:300-518`, `services/control_plane.py:745-789`.
- The active production pilot-serving operator surface is the static Next app under `meshapp/frontend`, served by `run_server.py` from `meshapp/frontend/out` and by the zero-native shell under `meshapp/src`. `web/` remains the Vite reference surface during migration. GPUI is archive material under `docs/history/gpui/`. Evidence: `docs/production-deployment-roadmap.md:23-26`, `meshapp/frontend/src/App.tsx`, `meshapp/frontend/src/api.ts`, `meshapp/src/main.zig`, `web/src/App.tsx:1293-1311`.
- The operator launch surface still exposes `native` and `promptfoo` evaluation modes, `native`, `hermes`, and `goose` orchestration modes, and `approval_gate` plus `interruptible_auto` steering through the migrated `meshapp/frontend/src/App.tsx` surface and the `web/` reference surface.
- The operator API client calls the same control-plane routes, including readiness, connector certification, approvals, kill switch, policy simulation, pilot go/no-go, run creation, Mesh Brain gates, run export, Darkharness packet, steering, events, Merkle, evidence graph, agent tasks, watchers, and vault. Evidence: `meshapp/frontend/src/api.ts`, `web/src/api.ts:119-180`, `web/src/api.ts:181-330`.
- JSON Schema files in `shared/mesh_runtime/schemas/` are the contract source of truth; Python contract dataclasses validate through `validate_payload`; generated UI contracts are checked by `scripts/generate_control_plane_contracts.py --check` for both `web/src/types.ts` and `meshapp/frontend/src/types.ts`. Evidence: `AGENTS.md:48-51`, `shared/mesh_runtime/contracts.py:11-26`, `shared/mesh_runtime/contracts.py:29-90`, `scripts/generate_control_plane_contracts.py:17-20`, `scripts/generate_control_plane_contracts.py:62-76`, `web/package.json:10-15`, `meshapp/frontend/package.json`.
- Runtime default state backend in `RuntimeConfig` and `RuntimeConfig.from_env()` is `file`; valid values are only `file` and `postgres`. Evidence: `shared/mesh_runtime/config.py:97-99`, `shared/mesh_runtime/config.py:388-390`, `shared/mesh_runtime/config.py:790-794`, `shared/mesh_runtime/state_store_factory.py:9-12`.
- Optional memory graph projection is separate from canonical state backend. `MESH_MEMORY_GRAPH_BACKEND=helix` mirrors verified memory records into HelixDB, while run sessions, event streams, Merkle roots, approvals, and pilot persistence remain on `file` or `postgres` until a full Helix state-store backend and equivalent readiness proof gates exist.
- Compose deployment defaults are different from library defaults. The local full stack and production compose files default Mesh to Postgres. Evidence: `docker-compose.stack.yml:328-333`, `docker-compose.prod.yml:18-24`.
- Readiness profiles normalize `prod` and `production` to `pilot`, not to a separate `prod` profile. Valid profiles are `local`, `staging`, `pilot`, and `expansion`. Evidence: `shared/mesh_runtime/config.py:797-809`, `docs/production-deployment-roadmap.md:214`.
- Mesh-native evaluation remains authoritative. Third-party evaluation frameworks are advisory lanes and cannot approve execution by themselves. Evidence: `docs/evaluation-integrations.md:1-20`, `docs/evaluation-integrations.md:52-78`.
- Hermes, Goose, Deep Agents, LatentMAS, and similar agent lanes are proposal, review, explanation, or advisory paths unless code and certification prove bounded authority. Evidence: `docs/production-deployment-roadmap.md:33-37`, `docs/ui-auto-canvas-workspace.md:33-47`, `shared/mesh_runtime/integrations.py:221-260`, `services/orchestrator/service.py:42-83`.
- In `auto` orchestration mode, the current adapter selection prefers Hermes when ready, then Goose, then native Hermes fallback. Evidence: `services/orchestrator/service.py:42-83`.
- `agentic-operator-core-main/` is source input only. The tracked provenance file marks it `runtime_posture: source-input-only`, `active_runtime: false`, `wholesale_copy_allowed: false`, and `comparative_claims_allowed: false`; its imported-path lists are empty. Evidence: `config/agentic-operator-source.provenance.json:1-28`, `config/agentic-operator-source.provenance.json:29-120`, `docs/agentic-operator-core-import-plan.md:55-65`.
- Release and readiness docs correctly warn that old or local evidence is not production proof. Evidence: `docs/production-readiness-validation.md:3-10`, `docs/production-readiness-validation.md:31-33`, `docs/production-readiness-validation.md:67-76`, `docs/production-readiness-validation.md:88-113`.
- `npm run lint` at repo root is wired and delegates to web lint plus meshapp frontend lint, which check generated contracts and TypeScript. Evidence: `package.json:1-6`, `web/package.json:10-15`, `meshapp/frontend/package.json`.
- Strict mypy is intentionally scoped to `services/decision/hypothesis_engine.py`; it is not whole-repo type proof. Evidence: `pyproject.toml:76-84`, `docs/production-readiness-validation.md:24`, `docs/production-readiness-validation.md:88-91`.

## Stale Or Risky Claims

- `architecture.md` starts with `# Mesh Intelligence Architecture` and states "`mesh-intelligence` is..." even though this checkout and package are `orbital-mesh`. This is stale naming unless treated as historical lineage. Evidence: `architecture.md:1-6`, `pyproject.toml:5-8`.
- `architecture.md` contains competitive and market-positioning material that is not proven by current code alone, including comparisons to OpenSRE and Resolve.ai and claims such as "edge", "strongest moat", and displacement framing. Treat those as positioning hypotheses, not operating truth. Evidence: `architecture.md:22-51`, `architecture.md:138-158`.
- `architecture.md:146` says "Postgres is production; SQLite + JSON files is local-dev." Current code supports file mode by default and Postgres by configuration, while compose production defaults to Postgres. The safer truth is: file mode is supported and default in `RuntimeConfig`; Postgres is the production compose default and required for multi-operator production reliance. Evidence: `architecture.md:142-148`, `shared/mesh_runtime/config.py:97-99`, `shared/mesh_runtime/config.py:388-390`, `docker-compose.prod.yml:18-24`.
- `README.md:185` references a vendored Evo command under `/workspace/orbital-mesh/evo/plugins/evo`; this checkout has no `evo/` directory, and AGENTS says Evo is no longer vendored in-tree. Use a separately installed `evo-hq-cli` through `MESH_EVO_COMMAND`. Evidence: `README.md:185`, `AGENTS.md:44-46`, `find . -maxdepth 2 -type d -name evo` returned no `evo` directory.
- `docs/production-deployment-roadmap.md:95` names readiness profiles as `local`, `staging`, `pilot`, and `prod`; code normalizes `prod` to `pilot`, and the implemented fourth profile is `expansion`. Evidence: `docs/production-deployment-roadmap.md:90-96`, `shared/mesh_runtime/config.py:797-809`, `docs/production-deployment-roadmap.md:214`.
- `README.md:63-72` lists supported steering commands but omits current commands `explain_blockers`, `chat_with_hermes`, `handoff`, `override_review`, and `postmortem_review`. Evidence: `README.md:63-72`, `services/control_plane.py:143-159`.
- `docs/ui-auto-canvas-workspace.md:123-129` says "No RBAC implementation yet." Current code and docs contain app-level role checks and readiness gates for proxy-stamped identity. This doc may be stale on that point; preserve the distinction that proxy identity remains the trust boundary. Evidence: `docs/ui-auto-canvas-workspace.md:123-129`, `docs/production-deployment-roadmap.md:212-214`, `control_plane_server.py:155-162`, `control_plane_server.py:550-650`.

## Conflicts Between Docs, Code, And Config

- State backend wording conflicts across docs. `architecture.md` implies local-dev uses SQLite plus JSON files, while `RuntimeConfig` calls the backend `file` and `state_store_factory.py` selects `FileStateStore`; compose defaults to Postgres. Future docs must say "file-backed local state" unless specifically referring to SQLite internals. Evidence: `architecture.md:142-148`, `shared/mesh_runtime/config.py:790-794`, `shared/mesh_runtime/state_store_factory.py:9-12`, `docker-compose.stack.yml:328-333`.
- Readiness profile naming conflicts. Roadmap text mentions `prod`, code accepts `prod` only as an alias for `pilot`, and hardening docs use `expansion` for the later tier. Evidence: `docs/production-deployment-roadmap.md:90-96`, `shared/mesh_runtime/config.py:797-809`, `docs/production-hardening-records.md:52-60`.
- Evo source assumptions conflict. README suggests a vendored path; AGENTS and filesystem evidence say Evo is external. Evidence: `README.md:185`, `AGENTS.md:44-46`.
- Architecture competitiveness sections conflict with the task rule against unsupported claims. Keep them out of future operating docs unless independently verified and scoped to benchmark evidence.

## Aspirational Or Not Yet Implemented

- Private staging and production pilot exit gates are mostly target-environment requirements, not current proof. Examples include three real operators, deployed SSO/TLS ingress proof, real webhook or OTel path creating runs, backup restore meeting declared RPO/RTO in target, signed go/no-go, complete release provenance, and target production drill evidence. Evidence: `docs/production-deployment-roadmap.md:86-170`, `docs/production-readiness-validation.md:31-33`, `docs/production-readiness-validation.md:101-107`.
- Helm, Terraform, marketplace packaging, and ingress-controller-specific packages remain gaps. Evidence: `docs/reference-architectures.md:1-4`, `docs/reference-architectures.md:145-150`.
- Agentic operator CRDs, controllers, Argo scheduling, tenant isolation, LiteLLM/MCP, Helm packaging, and agentctl workflow imports are plans until forked through authority gates and tests. Evidence: `docs/agentic-operator-core-import-plan.md:67-100`, `config/agentic-operator-source.provenance.json:10-28`.
- Durable external audit sink certification, external incident provider, and real feature-flag provider adapters are not production-complete just because proof schemas exist. Evidence: `docs/production-deployment-roadmap.md:254-260`, `docs/production-hardening-records.md:400-408`.
- Mesh Brain broad model-serving production remains blocked; roadmap says controlled MVP proof and gap reports remain. Evidence: `docs/production-deployment-roadmap.md:37`, `docs/production-deployment-roadmap.md:267-268`.

## Historical And Archive Areas

- `docs/history/gpui/` is archived GPUI provenance, not an active build or parity target. Evidence: `docs/production-deployment-roadmap.md:24-26`.
- `docs/history/research/` and Reth/Kurtosis records are historical research proof, not current release gates. Evidence: `docs/production-readiness-validation.md:33-44`, `docs/production-readiness-validation.md:67-76`.
- `.mesh-runtime-state/`, `.hermes-local/`, `.venv/`, `meshapp/frontend/out/`, `web/dist/`, `web/test-results/`, and similar local artifacts are ignored runtime or generated state. Do not treat them as committed release proof. Evidence: `docs/production-readiness-validation.md:75-86`.
- `deepagents/`, `latent-mesh/LatentMAS/`, and `agentic-operator-core-main/` are vendored or source-input areas by default. Do not refactor them unless an active Mesh contract requires it. Evidence: `AGENTS.md:42-46`, `pyproject.toml:29-30`, `config/agentic-operator-source.provenance.json:10-28`.

## Unknowns Requiring Live Validation

- Current compose stack status, current `/api/readiness`, and current `/api/pilot/go-no-go` must be queried live before any handoff that claims readiness.
- Current Docker build, SBOM, vulnerability scan, and GitHub Actions attestation must be regenerated before release provenance can be treated as current.
- Current external TLS/SSO ingress, header stripping, operator identity propagation, and private upstream boundary require target-environment proof packets.
- Current Kubernetes watcher, webhook, and OTel paths require live target proof before private staging claims.
- Current Postgres backup/restore, migration rehearsal, on-call drill, run-export upload proof, audit sink proof, and load/concurrency proof require target-environment artifacts.
- Synthetic, fixture, local compose, or `--allow-dirty` evidence can support development checks but cannot sign a pilot release or production claim.
