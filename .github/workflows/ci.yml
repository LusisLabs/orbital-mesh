name: CI

on:
  push:
    branches: [main, master]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install ruff
        run: pip install ruff==0.7.4
      - name: Lint
        run: ruff check .

  python-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run unit tests
        run: python -W ignore::DeprecationWarning -m unittest discover -s tests -v
      - name: Smoke-import RuntimeConfig
        run: python -c "from shared.mesh_runtime import RuntimeConfig; print('RuntimeConfig OK on', RuntimeConfig.from_env().environment)"

  simulation-matrix:
    runs-on: ubuntu-latest
    needs: [python-lint]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run small randomized simulation matrix
        run: python scripts/run_simulation_matrix.py --iterations 4 --workers 2 --timeout-seconds 20 --randomize --seed 20260424 --output .mesh-runtime-state/simulation-stress/ci-small

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - name: Lint
        run: npm run lint
      - name: Build
        run: npm run build
      - name: Test
        run: npm test --silent

  docker-build:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    needs: [python-test]
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build --build-arg MESH_BUILD_VERSION=ci-${{ github.run_number }} --build-arg MESH_BUILD_COMMIT=${{ github.sha }} -t mesh-intelligence:ci .
      - name: Healthcheck smoke test
        run: |
          set -euo pipefail
          docker run -d --name mesh-ci \
            -e MESH_SERVER_HOST=0.0.0.0 \
            -e MESH_SERVER_PORT=8787 \
            -e MESH_ENVIRONMENT=ci \
            -e MESH_GITNEXUS_DISABLE_AUTOSTART=1 \
            -p 8787:8787 \
            mesh-intelligence:ci
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8787/api/health > /dev/null; then
              echo "Server healthy after ${i}s"
              break
            fi
            if [ "$i" -eq 30 ]; then
              echo "Server failed to become healthy in 30s"
              docker logs mesh-ci || true
              exit 1
            fi
            sleep 1
          done
          curl -sf http://localhost:8787/api/health | tee /dev/stderr | python3 -c "import sys, json; assert json.load(sys.stdin)['status'] == 'ok'"
      - name: Capture container logs on failure
        if: failure()
        run: docker logs mesh-ci || true
      - name: Stop container
        if: always()
        run: docker stop mesh-ci || true

  swarmclaw-typecheck:
    if: ${{ hashFiles('swarmclaw/package.json') != '' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: swarmclaw
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: swarmclaw/package-lock.json
      - run: npm ci
      - run: npm run type-check
